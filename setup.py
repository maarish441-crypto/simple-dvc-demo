from setuptools import setup , find_packages

setup(
    name="src",
    version="0.0.1",
    description = "its a wine Q project",
    author = "maarish441",
    packages = find_packages(),
    license = "MIT"
)